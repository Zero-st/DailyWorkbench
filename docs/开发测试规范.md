# 开发测试规范 · 最小测试先行，全绿再跑全量

> 治理约定，与 [版本管理规范](版本管理规范.md)、[TECH_CHARTER](TECH_CHARTER.md) 同级。核心一句：**每个模块/接口先用最小测试验通，所有子路径都绿了，才跑一次整份数据/全量流程——避免反复全量，省 token、省成本、省时间。**
>
> 本文档管小改动这一档；新模块/新 seam/新外部依赖/多分支逻辑这类复杂改动，走 `tdd` skill 的红绿循环（`.claude/skills/tdd/SKILL.md`），别在这两处重复写判断标准。

## 1 · 为什么（第一性）

全量跑（全量 LLM 调用 / 全量抓取 / 全量重建）既慢又费钱，且一旦中途某个模块有坑，前面烧的 token 全白费、还要重来。**把验证左移到最小单元**：用一次调用、一条合成数据、一段纯逻辑就能证伪的，绝不用整份数据去证。最小测试是"不对称投资"——极小成本换取"这条线路到底通不通"的确定性。

## 2 · 规范（怎么做）

开发一个功能/接口/管线时，按下面顺序推进，**上一档不绿不进下一档**：

1. **静态门**（零成本）：语法/编译先过——后端 `py_compile`，前端 ES Module 语法门（`node` 动态 `import()` 只判 `SyntaxError`，`node --check` 对 ESM 有假阴性）；然后 `python check_all.py`（门禁聚合器，**清单唯一真源**，约 3 秒跑完六项）。
   不在这里枚举跑哪几个脚本——枚举一次就多一份会漂的副本（§3.1b 已记过这个坑）。
2. **纯逻辑单测**（零网络）：能用合成输入跑的分支逻辑（幂等标记、去重、边界、异常兜底），就地写个 inline 断言验，别拉真服务。
3. **单点接口最小测试**（一次调用）：外部依赖（LLM/向量库/HTTP 端点）先打**一条**——探维度、embed 一句、upsert 一行、curl 一个坏参数看是否优雅降级。**先证"通路 + 契约 + 降级"，再谈规模。**
4. **单模块小样**（≤ 一个批次）：管线里一步先跑 3~5 条，核对产出字段/缓存/幂等（二次跑应命中缓存、不重复烧）。
5. **全绿后，唯一一次全量**：以上都绿，才清标记/跑整份数据。全量前想清楚"这次全量到底为验证什么"，且尽量复用缓存（如只补向量、不重烧摘要）。

配套原则：
- **结合已知坑点**：验证清单里显式覆盖踩过的坑（见 §3），别只测 happy path。
- **可降级不阻断**：外部依赖缺失/失败，模块要优雅跳过、保留旧数据、跨轮自愈（缓存标记），而不是崩或静默丢结果。
- **成本护栏**：全量要有上限（每轮条数上限、缓存幂等），失败落盘可审、不无界重试。

## 3 · 已知坑点台账（验证时逐条对照）

### 3.0 · 升级判据 —— 为什么这是「台账」不是「清单」

> **同一个坑复发第 2 次 = 文档失效的实证，必须升级防护等级，禁止再写第 3 份文档。**

**缘起（一条被实证的教训）**：「emoji 渲染成豆腐块 ☒」2026-08-31 立了硬性红线（[界面设计准则](design/界面设计准则.md) §4），规则同时写进 **5 个文档**——之后仍复发 **3 次**（09-02 v0.9.1 批量迁移、09-20 D3 主按钮、09-21 进度视图状态列）。

[TECH_CHARTER](TECH_CHARTER.md) 维度三早就写下了答案：**「准则说的规矩必须有脚本查，否则和任何文档一样漂移」**——`check_design_tokens.py` 正是这么诞生的。缺的从来不是哲学，是**触发条件**。本节就是那个条件。

**三档防护**（与[心法](principles/开发心法-多维思维总纲.md) §4「绝不跳档」同一逻辑，只是对象从「功能」换成「防护」）：

| 档 | 形态 | 何时必须升到下一档 |
|---|---|---|
| **文档** 📄 | 写进准则/规范，靠人记 | 第 1 次踩坑的默认档。**复发第 2 次即判定失效** |
| **门禁** 🤖 | 机器查，会红。**两处**：① **会话内** —— Stop hook（`.claude/hooks/gate.sh`）在 agent 每轮收尾时全跑，红了不让它结束；② **CI** —— push 后的最终防线。新坑**优先挂会话内**（反馈快、agent 当场自己修），CI 兜底不可省（hook 只护 Claude 会话，手工改代码绕得过） | 复发第 2 次必须到这档。**查不干净就退一步**：把人工步骤写成**可粘贴的命令**，而不是一句「记得检查」 |
| **设计** 🏗 | 结构上不可能犯（接口上就没有那个危险操作） | 上了门禁还犯 |

**反向判据**（防门禁通胀，照宪章「回溯校验」）：**拦不住任何真实事故的门禁是废话，该删。**

**怎么用**：踩坑当天在 §3.1 加一行或把复发次数 +1；到 2 次就**当场**升级，别等复盘。

---

### 3.1 · 在办（📄 档 · **需人读，提交前扫这一段就够**）

> 这里只放**还靠人记**的坑。升上 🤖 门禁的条目移到 §3.1b 压成一行——
> **人读区体积必须封顶**：清单一膨胀，「提交前扫一遍」必然退化成走过场，
> 那就是宪章禁的 `|| true` 假绿的人肉版。条目升级即搬走，别留在这占篇幅。

| 坑点 | 复发 | 现在靠什么防 | 状态 / 下一步 |
|---|---|---|---|
| **外部数据直出未过渲染层**（markdown 语法在表格里漏出字面量） | 1 次 | 📄 准则 §4 | 与 emoji 同根因，已随其一并扩写；**再犯即上门禁** |
| **假 CI（`\|\| true` 恒绿）** | 1 次 | 📄 宪章维度三 | 已删净；再犯即上门禁 |
| **状态字段说谎**（页脚「每小时自动同步」而无 cron；CHANGELOG 写「crontab 两行已挂」而实际没挂） | **2 次** | 📄 | 🔴 **已达升级线，门禁待设计**——只登记不动手，避免混进别的批次 |
| **硬编码环境绑定**（`E:\` 路径，换机即废） | 1 次 | 📄 宪章维度三 + `wb_config` 三级读取 | 未再复发 |

> 「状态字段说谎」标 🔴 却**刻意只登记不动手**：它需要独立设计，塞进当前批次会让它变成「什么都做一点」的元工作。**台账的价值是让欠账可见，不是一次还清。**

---

### 3.1b · 已毕业（🤖 门禁盯着 · 一行索引，**不必逐条读**）

> 升级为机器门禁 = 人不用再记，所以这里只留一行。留这一行是为两件事：
> ①复发时能回溯根因；②给 §3.0 **反向判据**（「拦不住任何真实事故的门禁是废话，该删」）留下对象。
> 各门禁的命中次数：`python check_all.py --stats`。**注意零命中 ≠ 无用**——可能正因有威慑力而无人触犯，故该表只用于排序，删不删由人拍板。

| 坑点 | 复发 | 门禁 | 结算 |
|---|---|---|---|
| **emoji 豆腐块 · 数据里的符号直出页面** | **4 次** | `check_design_tokens.py` —— `index.html` emoji 硬门必须 0 | ✅ 2026-09-21 |
| **缓存戳漏同步 → 用户端吃旧缓存白屏** | 多次 | `bump_version.py --check` | ✅ 已久，未再复发 |
| **活文档漂移**（规则 3 份副本打架：治理「三篇」vs「两篇」、词表漏 4 类、README 的 ADR 枚举停在 0010） | 多次 | 规则收敛进 [文档规范](文档规范.md)（唯一真源）+ `check_docs.py` 四项硬门 | ✅ 2026-09-21 |
| **门禁清单自身出现副本**（`gate.py` 自带 4 项 vs `check_all.py` 6 项，建起来当天就漂：少了 flake8 与 tsc） | 1 次 | `check_all.py` 是清单唯一真源；`gate.py` / `.githooks/pre-commit` / CI 全部调它，不各自枚举 | ✅ 2026-09-22 |

> 「活文档漂移」的门禁边界：`check_docs.py` 拦得住「索引漏收 / 删文件留死链 / 野文件乱放 / 规则正文复制回 SKILL 或 README / 把摘要抄进索引」，**拦不住**大白话重写规则与索引里的数字断言过期——后者的可粘贴复核命令在 [文档规范](文档规范.md) §6.1。

---

### 3.2 · 取数 / 管线坑点（属 📄 档，验证时逐条对照）

来自本仓资讯语义检索（Zilliz + BYO embedding）实战，跨功能通用：

- **外部模型/端点先探通再规模化**：厂商 base 路径/模型 id/权限极易错（如火山方舟 **Coding Plan** 是 `/api/coding/v3` 且 chat=`glm-5.3`、embedding=`doubao-embedding-vision`(2048维)，普通 `/api/v3` + 通用模型名 404）。**先 1 条 embed/chat 打通，别直接全量。**
- **向量库 schema 用最小 schema 先建通**：Zilliz/Milvus REST v2 字段参数键是 `elementTypeParams`（非 `params`）；serverless DDL 慢（超时放宽到 90s）。**先建 id+vector 最小表验通，再补标量字段。**
- **大响应易 `IncompleteRead`**：高维向量 × 大批次（2048×10）响应体大、跨区连接易截断；靠"重试 + 缓存 e 标记跨轮自愈"，必要时**调小 embedding 批次**（响应减半）。
- **带密钥请求不得降级不校验 TLS**：抓公开内容可降级，但带 `Authorization` 的请求降级 = 泄 key，只在校验 ctx 下重试。
- **入参健壮**：接口对坏参数（非数字 topk 等）要兜底默认、返回 200/降级，而非抛异常落 500。
- **幂等标记只标真正成功项**：批处理里被跳过的项不能连带标记"已完成"，否则永久遗漏且不重试。

### 3.3 · 会话内门禁（Stop hook）—— 「写代码前先扫描」的正确形态

**缘起**：2026-09-21 用户问「踩过的坑是不是该单独写个文档，AI 写代码前先扫描、加进上下文」。
照 §3.0 判据，答案是**不**——坑点台账（§3.1）早就存在，而且「写文档让 AI 读」在本仓**有实测反证**：
emoji 那条规则同时写进 **5 个文档**、每次会话都自动注入，**仍复发 3 次**。
**读到 ≠ 遵守**；上下文里规则越多，每条权重越被稀释，再加一份反而更糟。

真正缺的不是文档，是**反馈时机**：三个门禁此前只在 CI 跑，而 agent 在会话里看不到 CI 结果。

**做法**：`.claude/settings.json` 把 `Stop` 事件挂到 `.claude/hooks/gate.sh` —— agent 每轮**收尾**时
自动跑 `check_docs` + `check_design_tokens` + `bump_version` + `pytest`，红了**把失败原文喂回去、不让它结束**。
实测单轮 **1.25 秒**，全绿时静默无感。

| 项 | 说明 |
|---|---|
| 防循环 | 输入 JSON 的 `stop_hook_active` 为真即放行；另有 Claude Code 兜底：连续 block 8 次强制放行 |
| 解释器 | 候选 `$WB_PYTHON` → 仓内 `.venv/venv` → `python3` → `python`，**优先选装了 pytest 的那个**。`gate.sh` 内**无任何机器绝对路径**（§3.1「硬编码环境绑定」那条坑）；本机路径放 `.claude/settings.local.json` 的 `env`（私有、不进仓库） |
| 无豁免开关 | 刻意不做「临时关掉」——豁免会让门禁失效。要停就显式删 `.claude/settings.json` 那段 |
| 假绿防护 | 解释器缺 pytest 时**如实报红**，不静默跳过（宪章维度三：禁「看着在测其实没测」） |

**实测依据**（非文档推断）：官方文档对 command 类型 hook 的 `reason` 去向没写死
（`continueOnBlock` 只出现在 prompt hooks 章节）。故先用一次性自解除探针实测，
确认 `{"decision":"block","reason":...}` **确实进入 agent 上下文并阻止回合结束**，
**不需要** `continueOnBlock`。照 §2「先最小测试，全绿再跑全量」。

**拦不住的**（别高估它）：
1. **只护「和 agent 的会话」**——手工改代码、别的工具、别人 clone 后没设解释器，都绕得过。**CI 仍是最终防线**，两者并存不是冗余。
2. 只拦**机器可判定**的坑。§3.1 剩下的「状态字段说谎」、大白话重写规则、数字断言过期，一样拦不住。
3. 这是 **🤖 档不是 🏗 档**：仍是「犯了立刻被抓」，不是「结构上不可能犯」。
4. 照 §3.0 **反向判据**：若它半年内没拦下任何真实事故，**该删**。

---

## 4 · 反例（别这么干）

- 上来就点"同步/全量"，管线中段某步坑了，整轮 token 白烧。
- 只在真全量里才发现"模型名错/字段键错/降级泄密钥"这类一条调用就能暴露的问题。
- happy-path 过了就宣布完成，不覆盖失败/边界/降级路径。
