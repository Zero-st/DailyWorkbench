# 复盘 · 项目自评打分 · 2026-09（第一张卡：v0.10.0）

> 日期：2026-09-17（首版 10:00 磁盘取证；**v1.1 16:49–16:57 真机复验回填**）　性质：活文档（项目自评，按 [`../guides/项目自评打分-指南.md`](../guides/项目自评打分-指南.md) 打）　基线：CHANGELOG **v0.10.0**（2026-09-03）· git HEAD `00b17bb`（2026-09-16）
> 关联：事前目标来源 [`复盘-dogfood冲刺-W1.md`](复盘-dogfood冲刺-W1.md) §1（定于 2026-09-03）· 质量层基线 [`../design/工作台走查评审-v0.8.0.md`](../design/工作台走查评审-v0.8.0.md) §1（2026-09-01）· 事前门 [`../principles/开发心法-多维思维总纲.md`](../principles/开发心法-多维思维总纲.md) §4 · 校准 [`../guides/judgment-practice操作指南.md`](../guides/judgment-practice操作指南.md) · 上一次体检 `.claude/plan/next-steps-dogfood-w1.md`（09-03）
> **自评者偏差自注**：自评者 = 建造者 = 唯一用户；指南与打分同日完成（踩指南 §9「新鲜感」条）。因此**客观项优先**（磁盘产物 / 文件状态 / git 历史 / localStorage 行为记录），凡只能凭感觉的一律 `N/A · 观点`、上限 3。
> **复验条件（v1.1）**：后端 8899 由本次拉起（`python -m backend.server 8899`）；chrome-devtools MCP 驱动**调试 Chrome**（profile `~/桌面/comtools/chrome/profile`，时区 America/Los_Angeles）在 `http://127.0.0.1:8899/index.html` 做六视图**只读**走查；**日常 Chrome**（Default profile）的 Local Storage 以只读副本解析（自制 LevelDB/snappy 解码器，只读键名与日期）；本机无其它浏览器 profile。**证据范围声明**：localStorage 证据 = 这两个 profile × `127.0.0.1` 源；后端日志显示本次 2 个 POST 均为应用自身发起（页面加载即 `POST /api/models` 云同步、资讯首挂载空 q 探针），数据文件 `inbox.local.json` / `_index.jsonl` mtime 未变，收尾快照与开工前逐字节一致。

---

## 1 · 结论一句话 + 总分

**一句话**：**硬门没过**。17 天里两个浏览器 profile 合计只有 3 天有使用痕迹（09-01 落卡、09-10 收藏一条资讯、09-11 日常 Chrome 最后一次写入），**最近 6 天为 0**；温故卡从落卡那天起再没被点开；dogfood 预注册的 4 条退出指标 0 条被回填。与此同时三周 93 个 commit、5 个 MINOR 级新能力。产品建了很多，人没在用——这张卡最有价值的产出仍是 §5 的下期事前目标。

| 项 | 值 | 说明 |
|---|---|---|
| **总分** | **0 / 100（硬门 · 频率门不过）** | 指南 §4：任一硬门为「不过」→ 不进加权打分 |
| 参考值（若门过） | **27**（100 × 0.267^0.6 × 0.65^0.4 × 0.7 = 26.7） | 结果层 3/3 计分，无需置信带 |
| G 硬门 | 频率门 **不过** · 飞轮门 过 · 人在环中门 过 | 见 §2 |
| O 结果层 | 0.27（复用留存 **1** · 飞轮闭环 **2** · 判断校准 **1**） | 复验后复用留存由 N/A 变为 1 |
| Q 质量层 | 0.65（产品功能 **4** · UI 视觉 **2.5** · 交互 **3.5** · 健壮 **3**，09-17 真机） | UI 因深色模式不可读从暂定 3.5 降到 2.5 |
| C 成本层 | 0.30（扣满） | 维护税 0.2 + 漂移/逃逸 0.1 |

> **v1.0 → v1.1 变化**：首版因后端未跑、无标签，复用留存 N/A、质量层沿 09-02 证据，只发布置信带 [27, 35]。复验把 N/A 量成了 1、把 UI 视觉从 3.5 打回 2.5，参考值落到区间下沿 27；同时频率门从「未判定」变成「不过」，总分归 0。**带子收窄的方式是证据把它压到了下界**。
> 与 v0.8.0 评分卡的 78 / 100 **不可比**。可比的只有质量层四维：09-01 基线 4 / 2.5 / 3 / 3.5 → 本期 4 / 2.5 / 3.5 / 3——UI 的坏点从「emoji tofu」换成了「深色模式不可读」，健壮性的坏点从「下架代码 404」换成了「同步停摆 + 幽灵卡」。

---

## 2 · 硬门（项目级，指南 §4 G）

| 门 | 状态 | 证据（取于 2026-09-17） |
|---|---|---|
| 频率门：本期真实**使用**工作台 ≥ 2 天/周？ | **不过** | **调试 profile**（真机读取）：`wb_recall` 唯一真卡 `lastReviewed=2026-09-01, box=0`；`wb_favs` 1 条（2026-09-10 13:58）；`wb_info_collapsed` 折叠 4 个源、dock 曾拖成 401×753、agent 模式开——有使用痕迹但集中在 09-01 / 09-10。**日常 profile**（LevelDB 副本解析）：该源 11 个键，`META last_modified = 2026-09-11 16:10`，键只有 `wb_tab`（WAL 中写入 135 次，说明切过很多次视图）、模型配置、agent 模式、资讯折叠；**没有** `wb_recall / wb_review_* / wb_todos / wb_notes / wb_favs`。两个 profile 合计有痕迹的日子 ≤ 3 天（09-01 / 09-10 / 09-11），**09-11 之后 0 天**。93 个 commit 是建造者活动，不算使用。 |
| 飞轮门：交付落在哪一环？ | 过 | 09-04 → 09-16 的 feat：OpenCLI/HN、GitHub Trending、PH/少数派、X 源、语义检索（**输入环** ×5）；蒸馏 fabric 化 + tier（蒸馏环）；MCP server、页面驱动 agent、AI 副驾 dock（工具层）。**复用环 0 项**（「相关时浮现」仍在 Unreleased）。 |
| 人在环中门 | 过 | 页面驱动 agent（ADR 0009）只读白名单 + 人工写闸；dock「带工具」模式文案自述「只读不写库」；蒸馏仍由人确认落卡。 |

---

## 3 · 八列卡

| 维度 | 事前目标（定于 2026-09-03） | 事后实际（取于 2026-09-17） | 证据来源 | 证据等级 | 分（1–5 / N/A） | 决策质量（A/B/C） | 下一步 · 放弃线 |
|---|---|---|---|---|---|---|---|
| **结果层 O（指数 0.6）** | | | | | | | |
| 价值 · 复用留存 | 温故卡触发**且被点开** ≥ 3 次 | **点开 0 次**。调试 profile `wb_recall` = `{"蒸馏库/2026-09-01/…AI-Skills….md": {box:0, lastReviewed:"2026-09-01", archived:false}}`——落卡当天起未再点「打开」、未标「有用」，首页温故卡显示「16 天没看」；日常 profile **无 `wb_recall` 键**（温故从未在那边被碰过）。docs / ADR / CHANGELOG 中无「卡 X 影响了决定 Y」实例。 | 真机 `localStorage`（两 profile）；`js/features/recall.js:174-188` 确认 `box>0` 才代表点过「有用」 | 行为记录（09-17 真机） | **1** | 目标 A / 测量 **C**（目标定了，采集只有 localStorage 且没人看） | 先定「在哪个浏览器 profile 用工作台」，再补第 2 档导出脚本；放弃线见 §5-5 |
| 飞轮闭环完成度 | 蒸馏库真卡 ≥ 5；收件箱→已蒸馏转化率「只记录」 | **区间流量 0**：`蒸馏库/2026-09-01/` 1 张真卡（区间前）、`2026-09-09/` 目录为空（MCP 自测卡已删，ledger 残留 1 行）。**存量 1**（补测，非区间成绩）。收件箱 `inbox.local.json` = `[]`（mtime 09-03）→ 转化率 **0 / 0**，真机 `GET /api/inbox` 亦为空、UI「0 / 0 条」。五环曾用真实内容整转 1 次（[`复盘-MVP闭环首跑`](复盘-MVP闭环首跑.md)，09-01）。复验补充：`/api/kb/deposits` 返回 **2 条**，其中 1 条**幽灵**（文件不存在）→ UI「2 张卡」虚高一张（缺陷 D1）。 | `devmd/_index.jsonl`（3 行）+ 目录 `ls`；真机 `/api/kb/deposits` 逐条 `os.path.exists` 核对 | 行为记录 | **2** | 目标 A / 测量 C | 30 天真卡流量 ≥ 4；连续 30 天为 0 → 蒸馏库进「删除测试」评审 |
| 学习 · 判断校准 | 摩擦清单 ≥ 5 条；周末写出「下一个功能是 X，因为摩擦 Y 出现 N 次并过了三门」 | **预注册成功、测量环缺失（unresolved）**：复盘 §2 日志 7 行、§3 摩擦、§5 结论**全部空白**。预注册指标 **0 / 4** 被测量。复验补充：两个 profile 都没有任何 `wb_review_<date>` 键 → 「今日复盘」**写过 0 天**；`wb_todos / wb_notes` 亦为空。预测卡 1 张、决策日志 1 条（均 09-16、均关于 skill 自身）。 | `复盘-dogfood冲刺-W1.md` 文件状态；`.claude/skills/judgment-practice/data/*.md`；真机 `localStorage` 键名 | 行为记录 | **1** | 目标 **A** / 测量 **C** | 任一 7 天窗口回填 ≥ 5 天；再空白 → 承认不做 dogfood，删空表 |
| **质量层 Q（指数 0.4）** | 基线 09-01：4 / 2.5 / 3 / 3.5 | 09-17 真机：6 视图 `switchView` 全部渲染；55 个请求全 200（唯一 404 是我故意打开幽灵卡）；console 0 error / 0 warn（同上唯一例外）；30s `HEAD data.json` 轮询正常 | chrome-devtools `list_network_requests` / `list_console_messages` | 行为记录（09-17 真机） | | | |
| 产品功能 | — | 闭环组件齐：今日（温故/待办/专注/复盘）、资讯 6 源 2026-09-10 数据 + 词面过滤（「MCP」1/98 命中）+ 语义输入框、收件箱「已连后端」、知识库 **306 篇**（09-01 为 251）树 + 检索 + 阅读器（真卡 7 个小节渲染）、蒸馏库平台/tier 筛选 + 卡阅读、模型管理 1 个模型、AI 副驾 dock（普通/带工具、记忆库 0 条）。仍缺「相关时浮现」；X 源在 `data.json` 无数据（有标题无内容）。 | 真机 DOM 文本 + 截图 | 行为记录（09-17 真机） | **4** | B（功能多为「先做出来」，无量化目标） | 下次打分前给 X 源与语义检索定验收句 |
| UI 视觉 | — | 浅色态干净一致：侧栏/知识库树/蒸馏库/资讯全 SVG 图标（树 390 个 SVG、0 emoji），U1/U2 修复成立。**但**：① **深色模式文字不可读**——`--ink` 在 dark 下仍为 `#1f2935`，h1/卡标题对比度 **1.14:1**、按钮 **1.17:1**、正文 2.95:1（WCAG AA 要求 4.5:1），整屏暗字暗底（缺陷 D2）；② 首页仍用 emoji 做图标（📌 今日完成 / ➕ 添加 / 💾 保存复盘 / 📥 存知识库），本机渲染成方框（缺陷 D3）。 | 真机 `getComputedStyle` 采样 + 深浅两态截图 | 行为记录（09-17 真机） | **2.5** | A（准则 + 令牌门禁 + 效果图先行）→ **结果差**：门禁查「引用未定义变量」不查对比度 | 修 D2 后按准则 §6.0 深浅两态各截一张复验 |
| 交互体验 | — | PC 上切视图即时、筛选片/词面过滤/dock 开合顺手；X1–X4 修复成立（移动端已删、主题可切）。**摩擦**：「今日复盘」块空态仍显示待办文案「今日还没有待办，写一条吧～」（走查评审 X5 未修）；「今日代办」空态文案重复两行（09-01 已记）；温故/蒸馏库列出幽灵卡，点开得「读取失败或不在库内」（劣化文案友好，但卡本不该在）；主题按钮三态循环 light→system→dark 无提示。 | 真机 DOM 文本 + 截图 | 行为记录（09-17 真机） | **3.5** | A（ADR 0005 有取舍与门性） | X5 / 重复空态 / 幽灵卡三项收尾 |
| 技术健壮性 | — | **好的**：console 零报错；Supabase DNS 不可达时后端打 `[sb] get/upsert` 错误、前端从 `wb_models_v2` 兜底仍显示「已配置」（优雅劣化）；UI 左下角**有**同步陈旧告警「上次 9-10 16:40，已超时约 153.2 小时」（首版写「停摆未被任何告警发现」**有误**，特此更正：告警在，只是没人打开页面）。**坏的**：① `deposits` 只读 `_index.jsonl` 不校验文件存在 → 幽灵卡进列表与温故（D1）；② 同步 runner 停摆 7 天，`sync.intervalHours:1` 与页脚「每小时自动同步」说谎（D6）；③ `data.json.kpi` 全 0（聚合器 SQLite 不可达）；④ 服务端无日志文件（`server.py:417` 只写 stderr）；⑤ 每次页面加载即 `POST /api/models`，离线时静默失败（D5）。 | `list_network_requests`、后端 stderr 日志（本次落 scratchpad）、`data.json.sync`、`local_refresh.log` | 行为记录（09-17 真机 + 管道状态） | **3** | B | 对表时 `sync.lastRun` 距今 < 2h；修 D1 |
| **成本层 C（扣减，上限 0.3）** | | | | | | | |
| 维护税 | — | 本期新增长期负担 ≥ 2：Zilliz 云向量库（ADR 0011）、grok-cli / xAI 付费 key 路径（ADR 0012）、OpenCLI 外部 CLI（Node ≥ 20，ADR 0007）、in-repo MCP server（ADR 0008）。均有 ADR 与劣化/回滚设计。 | `docs/adr/0007–0012` | 文档 + 代码 | **−0.2** | A | 下次打分逐项问「删除测试」 |
| 逃逸缺陷 · 文档漂移 | — | ≥ 2 处：① `../design/知识库沉淀存储方案.md` §2 depositRoot 写 `E:\...` / `kb.local.json`，现实是 `workbench.local.json` → devmd 仓根；② `CHANGELOG.md` 停在 09-04，其后 13 个 commit 未记；③ 复验新增 D1 幽灵卡、D2 深色不可读两处逃逸缺陷。 | 文件对读；`git log -1 -- CHANGELOG.md`；真机 | 行为记录 | **−0.1** | C（无人负责「文档防漂移」与真机回归） | 修存储方案 §2；补 CHANGELOG；修 D1/D2 |

**补充代理指标**（未预注册，只记录不计分）：调试 profile 9 个键 / 日常 profile 11 个键；日常 profile 最后写入 2026-09-11 16:10；`wb_tab` 在日常 profile WAL 中写入 135 次（切视图很多次，日期不可考）；资讯源折叠 4 个（dnews/sspai/ph/gt）；收藏 1 条（09-10）；AI 对话历史 0、长期记忆 0。

**节奏背景**（只记录，不折分）：93 commit / 16 个活跃日（08-26 → 09-16）；类型分布 docs 26 · feat 21 · refactor 17 · fix 11 · style 6 · 其他 12。**dogfood 区间 09-04 → 09-10 共 15 个 commit，其中 feat 9 个**——复盘 §0 规则写的是「唯一允许的代码改动：阻断闭环的 bug」。文档 46 篇 vs 代码约 10.2k 行（js 4530 + py 4571 + css 1153），比 09-03 体检时（20 篇 vs 5.2k 行）两侧都翻倍。

### 3b · 复验发现的缺陷清单（只记不修，修代码不在本批范围）

| # | 缺陷 | 证据 | 影响维度 | 修法提示 |
|---|---|---|---|---|
| D1 | **幽灵卡**：已删的 `【MCP测试】kb_save-20260909-113515` 仍出现在蒸馏库与首页温故卡；点开 → `GET /api/kb/note` 404 →「读取失败或不在库内」 | `/api/kb/deposits` 返回 2 条，`os.path.exists` 为 False 1 条；`蒸馏库/2026-09-09/` 目录为空 | 飞轮闭环（计数虚高）、健壮性、交互 | `backend/clients/kb.py` 的 deposits 读账本时过滤不存在的文件，或删卡时同步删 ledger 行 |
| D2 | **深色模式文字不可读**：`--ink` 在 dark 下仍 `#1f2935`；h1/卡标题 1.14:1、按钮 1.17:1 | 真机 `getComputedStyle` + 深色截图 | UI 视觉（主因） | 查 `css/styles.css` 深色令牌覆盖是否漏了 ink 系；给 `check_design_tokens.py` 加一条对比度门 |
| D3 | 首页残留 emoji 图标（📌/➕/▶/↺/💾/📥）渲染成方框 | 首页截图 + `emojiInHome=true` | UI 视觉 | 按界面准则 §4 迁内联 SVG（项目总览已列为增量） |
| D4 | 「今日复盘」空态显示待办文案；「今日代办」空态重复两行 | 首页 DOM 文本 | 交互 | 走查评审 X5 收尾（`js/views/dash.js`） |
| D5 | 页面每次加载即 `POST /api/models`，Supabase 不可达时后端 `[sb] upsert` 报错、前端无任何提示 | 后端日志 16:51:10 POST + `[sb]` 两行 | 健壮性（静默劣化也是盲点） | 只在配置变更时 upsert；失败给一次性 toast |
| D6 | 页脚「本机数据每小时自动同步」+ `sync.intervalHours:1`，实际 7 天未跑（左下角告警在，但没人看） | `data.json.sync`、`local_refresh.log` 6 次运行 | 健壮性、文档防漂移 | runner 常驻或改手动触发并改文案 |
| D7 | `kbSearch("蒸馏")` 程序化调用后树与计数无变化 | 真机调用前后 `#kbCnt` 均 306 篇 | 待确认（可能需先填 `#kbQ`） | 下次走查用真实输入框验证 |
| 注 | 调试 Chrome 时区为 America/Los_Angeles（systemd 用户服务无 TZ），头部时钟显示 01:5x——**环境差异非应用缺陷**，但会让该 profile 下的 `wb_review_<date>`、「N 天没看」按 LA 日期计算 | `Intl.DateTimeFormat().resolvedOptions().timeZone` | — | `chrome-mcp.service` 加 `Environment=TZ=Asia/Shanghai` |

---

## 4 · 决策质量 vs 结果质量（本期关键决策）

| 决策（日期） | 决策质量 | 结果质量 | 一句话 |
|---|---|---|---|
| MVP 聚焦飞轮五件，9 视图 → 6（09-01，v0.8.0） | **B**：过三门、方向清楚，但没定「聚焦后要看到什么数字」 | 未测 | 好决策，没配量尺 |
| dogfood 冲刺 W1 + 只修阻断 bug（09-03） | 目标 **A** / 测量 **C** | **未执行**：日志空白；区间内 9 个 feat commit；两个 profile 的复盘键为 0 | 一人项目最典型的「舒服帽挤掉难受帽」（心法 §5），这次被自己的规则原地抓到 |
| 止血批次：删 `lite/` + xlsx、修 bump_version 漂移、补 kb.save 测试（09-03） | **A**：清单具体、可验 | **好**：回归测试 ×6 入库、CI `--check` 真门禁 | 工程帽的活做得干净 |
| 设计契约先行 + 令牌门禁进 CI（09-04） | **A**：准则、效果图门禁、CI 三件齐 | **差**：深色模式对比度 1.1:1 仍能过门禁——门禁查「引用未定义变量」不查「可读性」 | 决策质量高、结果质量差的教科书例子：量尺量错了东西 |
| 松绑「零依赖」+ 接 Zilliz 语义检索（09-10，ADR 0010/0011） | **A**：写了 ADR、留了回滚、契约变更标单向门 | 未测：语义检索被用过几次无记录；维护税 +1 | 决策流程好，缺「谁用、用几次」的验收句 |
| 接 X 源经 grok-cli（09-14，ADR 0012） | **B**：有 ADR，但「Agent 输出解析未真机校准」自己也标了最脆 | 未验：真机 `data.json` 无 X 数据，页面只有标题 | 输入环再 +1，复用环仍 0 |

**规律**：六条决策里五条决策质量 A/B，结果质量却有五条「未测 / 未执行 / 差」。问题不在判断，在**测量环缺失**——这正是学习·判断校准维度只有 1 分、频率门不过的共同根因。

---

## 5 · 下期事前目标（2026-10-17 对表）

| # | 可观察指标 | 目标 | 放弃线 | 怎么量 |
|---|---|---|---|---|
| 0 | **先定「在哪个浏览器 profile 用工作台」**（日常 Chrome Default，或调试 Chrome） | 写进本表并固定 | — | 没有这一条，#5 永远量不到 |
| 1 | 真卡**流量**（09-17 → 10-17 新增、排除 `【MCP测试】`） | ≥ 4 | 30 天为 0 → 蒸馏库进「删除测试」评审 | `_index.jsonl` 过滤 `module=蒸馏库` 且 `savedAt` 在区间 |
| 2 | 收件箱条目 / 已蒸馏 | ≥ 5 条且 ≥ 2 条 `已蒸馏` | 0 条 → 浏览器扩展进「删除测试」评审 | `inbox.local.json` 的 `status` 字段 |
| 3 | 资讯同步存活 | 对表时刻 `sync.lastRun` 距今 < 2h，且区间内 `ok=True` ≥ 20 次 | runner 无法常驻 → 改手动触发并改掉 `intervalHours` 语义与页脚文案 | `data.json.sync`；`backend/pipeline/local_refresh.log` |
| 4 | dogfood 日志回填 / 「今日复盘」 | 任一 7 天窗口 ≥ 5 天有日志行，且所选 profile 出现 ≥ 5 个 `wb_review_<date>` 键 | 再空白 → 承认不做 dogfood，删空表、改路线 | 复盘文件内容 + 本文复验同款 localStorage 快照脚本 |
| 5 | 温故点开 | 所选 profile 的 `wb_recall` 中 ≥ 3 张卡 `box>0` 或 `lastReviewed` 晚于落卡日 | 10-17 仍无 → 该指标从表中删除 | 同上快照脚本（调试 Chrome 走 chrome-devtools `evaluate_script`；日常 Chrome 走 `ldbdump.py` 解析副本） |
| 6 | 产品判断预测卡 | ≥ 5 张关于产品判断（非 skill 自身）且到期回填 | — | `.claude/skills/judgment-practice/data/predictions.md` |
| 7 | 复验缺陷 D1（幽灵卡）、D2（深色不可读）修复并真机验证 | 两项均关闭，深色态 h1 对比度 ≥ 4.5:1 | — | 同款走查脚本 + `getComputedStyle` 采样 |

> 记成预测卡：`/judgment-practice` 分支 1，一条目标一张卡（本文不代写数据文件）；10-17 用分支 2 回填，再打第二张卡 `复盘-项目自评打分-2026-10.md` 与本文并排看校准。

---

## 6 · 复盘（做对 / 踩坑 / 下一步）

- **做对**：事前目标真的存在（09-03 预注册了 4 条），这次才有东西可对表；证据全部来自磁盘 / git / 文件状态 / localStorage 行为记录，没有一格是编的。复验守住只读纪律：不触发任何写函数与外部付费调用，2 个 POST 均为应用自发，数据文件 mtime 未变，主题/dock/标签键收尾还原到开工前的值。
- **踩坑**：① 测量环缺失——目标定了没人量、没采集机制；② 工程帽偷跑——dogfood 周里 9 个 feat；③ 状态字段说谎——`intervalHours:1` 而 7 天没跑；④ **首版有一处事实错误**（「停摆未被任何告警发现」），真机一开就发现左下角有告警，教训：没开过页面就别断言 UI 没有什么；⑤ 复验本身的坑：调试 Chrome 服务已死要先 `systemctl --user start`，其时区是 LA；`new_page` 对这个 SPA 会卡，改「开 `about:blank` → 脚本设 `location.href`」；LevelDB 解析库装不上，自己写了 100 行解码器（在 scratchpad，不进仓）。
- **下一步**：按 §5 走，第 0 条最先；下次打分前先把「怎么量」补齐，否则第二张卡不会比这张好看——好看不是目的，**量得到**才是。

---

## 7 · 与其它文档的分工

| 文档 | 管什么 |
|---|---|
| **本文** | 2026-09 这一张卡：数字、证据、缺陷清单、下期目标 |
| [`../guides/项目自评打分-指南.md`](../guides/项目自评打分-指南.md) | 刻度、算法、模板（改规则去那里） |
| [`复盘-dogfood冲刺-W1.md`](复盘-dogfood冲刺-W1.md) | 事前目标真源（本文只引用，不回填它的空表） |
| [`../design/工作台走查评审-v0.8.0.md`](../design/工作台走查评审-v0.8.0.md) | 质量层基线与取证方法；本文 §3b 缺陷编号 D1–D7 与其 X/U/T 编号互不重叠 |
| [`../guides/judgment-practice操作指南.md`](../guides/judgment-practice操作指南.md) | 下期目标 → 预测卡的操作 |

## 8 · 变更记录

- 2026-09-17 · v1.1 · **真机复验回填**（chrome-devtools MCP 六视图只读走查 + 两个 Chrome profile 的 localStorage 证据）：频率门 未判定 → **不过**，总分改为 0（参考值 27）；复用留存 N/A → 1；UI 视觉 3.5 → 2.5（深色模式对比度 1.1:1）；新增 §3b 缺陷清单 D1–D7；更正首版「同步停摆无告警」的错误陈述；§5 加第 0 条与第 7 条。
- 2026-09-17 · v1.0 · 首张卡。取证条件受限（后端未运行、无已开标签），结果层覆盖 2/3、总分只发布置信带 [27, 35]。
