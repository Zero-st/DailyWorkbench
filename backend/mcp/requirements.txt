# DailyWorkbench MCP 集成层的**隔离依赖** —— 只有运行 backend/mcp 才需要。
# App 核心（backend/server.py + 前端 SPA）不 import backend.mcp，故装不装本文件
# 都不影响「git clone 就能跑 / python -m backend.server 起本地服务」的核心形态
# ——核心不引新依赖、可离线跑（北极星：零构建·依赖极简且可撤回·本地起服务即可跑）。
# 安装：<anaconda-python> -m pip install -r backend/mcp/requirements.txt
# 代码用的是 mcp 2.x API（MCPServer；v1 的 FastMCP 已改名）。钉在 2.x，避免装到 v1（无 MCPServer）或 v3 破坏性变更。
mcp>=2.2,<3
