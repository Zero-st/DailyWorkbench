# DailyWorkbench · 开工先读（只做指针，规则不在这里重复）

1. **文档归位**：`docs/文档规范.md` 是规则唯一真源（分类树 / 命名词表 / 决策表 / 生成五步）——按它落位，落位后补 `docs/README.md` 索引一行（≤200 字符）。`python check_docs.py`（CI `--check` 会红）。**别处只许放指针，不抄规则正文。**
2. **工程红线**：`docs/TECH_CHARTER.md`——零构建 · 依赖极简且可撤回 · 本地起服务即可跑（离线优先；ADR 0010）；禁 `|| true` 假绿；改 `data.json` 契约 / 引入构建步骤 = 单向门（写 ADR）。
3. **动前端必读**：`docs/design/界面设计准则.md` = 本仓的 DESIGN.md。
   - 新视觉语言 / 新组件类 / 新布局骨架 → **先按准则 §6.0 出一页 HTML 效果图过目，再写代码**；沿用现成类的小改不用。
   - 只用 token 变量，不硬编码颜色，不写裸像素；图标内联 SVG（准则 §4）。
4. **开工业务功能**：`docs/guides/业务功能开工-图先行指南.md`——按不确定性分档，先思维导图捋业务 → 再流程图/时序图定实现思路 + 先定契约缝，再写码（软自检，小改免图）。收尾对接 `docs/guides/功能图解复盘-指南.md`。**需求登记**（非免图档）：`docs/requirements/` —— 现状改 `需求台账.md` 一行，过程追加 `需求演进史.md` 一条。
5. **改了 `css/ js/ index.html` 任何资产** → 跑 `python bump_version.py`（CI `--check` 会红）。
6. **门禁**：`python check_all.py` —— **清单的唯一真源**（缓存戳 · 设计令牌 · 文档 · flake8 · pytest · 类型契约，约 3 秒）。两个触发点跑的是同一份：**Stop hook**（`.claude/hooks/gate.sh`，我每轮收尾自动全跑，红了不许结束）· **pre-commit**（`.githooks/pre-commit`，任何提交前拦；逃生门 `git commit --no-verify`，代价是 CI 仍会红）。**别在别处枚举跑哪几个脚本**——副本当天就会漂（`docs/开发测试规范.md` §3.1b 记过）。要停 Stop hook 就显式删 `.claude/settings.json` 那段。
7. **踩坑先登记**：`docs/开发测试规范.md` §3.1 **在办**台账——踩一次复发数 **+1**，**到 2 次当场升门禁**（§3.0 三档判据），升完**把条目搬去 §3.1b 已毕业压成一行**（人读区体积必须封顶）。台账是**复发计数器**，不是给人读的备忘录；**别再写一份坑点文档**（emoji 那条写进 5 个文档仍复发 3 次，这是实证）。
8. **心法**：`docs/principles/开发心法-多维思维总纲.md`——每一步只戴一顶帽子，想法阶段别跳到写代码。
9. **开工先开当期作战板**：`.claude/plan/作战板-<期>.md`——做完就勾（勾 `[x]` 前对 DoD 四款）、做了计划外的事就在 §⑤ 记一行。完成度 = §② 勾选数/总数。规则见 `docs/guides/项目管理-操作指南.md`；commit 可加尾行 `Refs: R28, W1-3` 回指需求/任务。
