# 作战板 · 对表冲刺 2026-09-20 → 10-17

> 性质：**当期数据载体**。规则见 [`../../docs/guides/项目管理-操作指南.md`](../../docs/guides/项目管理-操作指南.md)，为什么这么排见 [`对表冲刺-2026-09.md`](对表冲刺-2026-09.md)。
> 建板：2026-09-21　对表日：**2026-10-17**
> 周表：[复盘-dogfood冲刺-W2-W4.md](../../docs/planning/复盘-dogfood冲刺-W2-W4.md)
> **完成度不在这里手抄**——§② 的勾选框本身就是真源。数字看工作台「项目进度」视图，或 `python -m backend.pipeline.usage_report`。此处一度抄过 `2/6`、`1/5`，那是第二真源，必然漂移（指南 §2 明写「别在这里维护第二份」，我自己先犯了）。
> 纪律：②③ 条目**只搬不重写**；勾 `[x]` 前逐条对 DoD 四款（指南 §3）。
> **上面两行「建板 / 对表日 / 周表」是机器要读的**（`backend/clients/progress.py`）——格式别动，内容随期改。

---

## ⓪ 计划总表

> **机器要读这两张表**（`backend/clients/progress.py`）——**表头是契约，别改字**。内容随现状更新，表头不动。

### 层一 · 项目阶段（方向 · 源：[`../../docs/planning/知识飞轮-路线图.md`](../../docs/planning/知识飞轮-路线图.md) §3）

| 阶段 | 内容 | 门性 | 状态 | 完成日 / 依赖 |
|---|---|---|---|---|
| Phase 1 | up 主经验蒸馏库（纯本地零云） | 双向门 | ✅ | 2026-08-31 完成 |
| Phase 2 | 本地持久化做扎实 + 备份兜底 | 双向门 | 🔧 | 捕获侧 09-03 已落地；`vault_backup.py` 在、**cron 未挂** |
| Phase 3 | 云端薄收件箱 + 多端实时同步 | 含单向门 · 先写 ADR | ⏸ | 本期明确推迟；依赖 R08 |
| Phase 4 | 手机全功能 + 出口闭环（压轴） | 较大门 | 🚧 | 依赖 Phase 3 |

### 层二 · 当期四周（执行 · 源：[`对表冲刺-2026-09.md`](对表冲刺-2026-09.md)）

| 周 | 区间 | 计划内容 | 周门 | 状态 |
|---|---|---|---|---|
| W0 | 09-20 → 09-22 | 清障碍 + 装尺子 | 出口门禁 6 条全绿（§②） | 🔧 |
| W1 | 09-23 → 09-29 | 每日 3 分钟契约 + 蒸馏 2 张真卡 | ≥4 天 `app_use` 且 ≥3 天 `review_save` | ⏳ |
| W2 | 09-30 → 10-06 | **待 W1 摩擦决定**（最多一件 · 第 3 档最小版） | 任一 7 天窗口 ≥4 天有交互 | ⏳ |
| W3 | 10-07 → 10-13 | 验 W2 那件 + 门禁补课（D2 深色令牌） | 7 天窗口 ≥5 天有交互 | ⏳ |
| W4 | 10-14 → 10-17 | 停手周 · 只对表 | — | ⏳ |

> 「状态」列**只放状态符号，不写 `2/6` 这类数字**——那是 §② 勾选框的派生值，写在这里就是第二真源。
> ⚠️ **W2/W3 的「计划内容」是冲刺故意留空的**（"按摩擦决定，最多做一件"）。这里只标「待 W1 摩擦决定」，**不编任务填满它**——编了就是把一张诚实的表变成装饰。

---

## ① 本期承诺（基线 · 期中不改）

**交什么**：不是功能，是**「这工具真的被用了」这件事本身**。

1. 清掉「打得开、有东西、说真话」的障碍（W0）
2. 装好尺子并让它出非空数（W0）
3. 每天 3 分钟契约跑满四周，每周蒸馏 2 张真卡给温故池装货（W1–W4）
4. 按摩擦决定加什么，**每周最多一件且必须是第 3 档最小版**（W2–W3）
5. 10-17 打第二张自评卡，**触发放弃线的当场执行**（W4）
6. 10-17 冲刺末**发 `v0.11.0` 并打 tag**（`[Unreleased]` 已堆 18 天且含 MINOR 级新增 `/api/usage`；规则见 [`../../docs/版本管理规范.md`](../../docs/版本管理规范.md) §4.1）

**明确不交**（搬自冲刺计划负向清单，不重写理由）：新增任何产品功能（含 R05）· Phase 3 云收件箱 · 往 `data.json` 加键 · 使用统计视图 · 新增资讯源 / 扩语义检索 / 修 X 源 · 救 self-hosted runner · 架构评审候选 2–6 · 把报表自动写进 docs。

> **2026-09-21 改基线**：原「明确不交」含「dogfood 完成度仪表」与「使用统计视图」，已按用户决定放行为「项目进度」视图（见 [ADR 0016](../../docs/adr/0016-in-app-progress-view.md)）——它置顶放弃线倒计时而非成就展示，且给自己预注册了放弃线。**§① 第 1 条「新增产品功能 = 0 个」因此不再成立**，本期实际新增 1 个，已记入 §⑤。

---

## ② 出口门禁（W0 → W1 · 完成度的分母）

> 冲刺计划原文：**6 条全绿才算 W1 可以开始**；第 6 条不过，后面三周全是白跑。

- [x] **1 · 备份仓公私已确认，备份策略已据此选定** — 验收：vault 的 origin 为公开仓，故 `vault_backup.py` 缺省**只提交不 push**，决策记于 CHANGELOG `[Unreleased]`。
- [~] **2 · 后端 systemd 自启已验证 + 浏览器启动页已固定** — 验收：`systemctl --user is-enabled/is-active workbench.service` → `enabled`/`active`，`loginctl` → `Linger=yes`（2026-09-21 实测）。**浏览器启动页在仓外，未验证**。
- [ ] **3 · crontab 两行已挂，`local_refresh.log` 出现非手动的整点记录** — 2026-09-21 实测**未达成**：`crontab -l` 无本项目条目、`systemctl --user list-timers` 为 0、日志停在 `2026-09-20 15:03:44`、`vault_backup` 从未产出日志。⚠ **CHANGELOG 已写「本机 crontab 两行」与实况不符**（见 §⑤ 偏离 1）。
- [x] **4 · ADR 0015 + 量测四件 + 测试全绿 + `bump_version.py` 已跑** — 验收：`docs/adr/0015-*.md` 在仓，四件齐（`clients/usage.py`·`pipeline/usage_report.py`·`js/core/usage.js`·`usage.local.jsonl` 已 gitignore），`pytest -q` **81 passed**（2026-09-21）。
- [~] **5 · D1 幽灵卡 / 温故空态 / D4 文案已修并真机验过** — 代码侧已改（CHANGELOG `[Unreleased]` Fixed 段 + `test_kb_list_deposits_skips_ghost_rows`）；**「真机验过」无证据**，待一次 chrome-devtools 走查。
- [~] **6 · 报表能打出非空表；点一次温故「有用」后报表第二次能看见** — 前半达成（2026-09-21 报表输出非空）；**后半未达成**：埋点至今只有 `app_use 1 / recall_open 1 / report_run 4`，**无 `recall_useful`**。

---

## ③ 本周任务（≤5 条 · 2026-09-21 → 09-22 · 补 W0 缺口）

- [ ] **W0-1 挂上两条定时任务**（**归属：用户自己挂**，2026-09-21 决定）— 验收：`crontab -l` 见到 `local_refresh` 与 `vault_backup` 两行，且次日 `local_refresh.log` 出现**非手动的整点**记录。⚠️ 本机系统时区是 America/Los_Angeles 而使用者是 Asia/Shanghai（差 15 小时），**不加 `CRON_TZ=Asia/Shanghai` 则 22:30 备份会在 CST 次日 13:30 触发**；cron 的 PATH 极简，需显式 `export PATH` + 绝对解释器路径（照冲刺计划 0-D 段）。**这条不绿，W1 按规则不能于 09-23 开工。**
- [x] **W0-2 订正 CHANGELOG 的不实陈述** — 验收：`[Unreleased]` 那条已改为「脚本已落地，调度尚未接管」并附实测证据（`crontab -l` 无条目 / `list-timers` 为 0 / 日志停在 09-20 15:03），commit `60e7654`。
- [ ] **W0-3 真机走查关闭门禁 5** — 验收：温故卡不再出现幽灵卡、无到期卡时显示提示而非整块消失、`list_console_messages` 零新报错。
- [ ] **W0-4 点一次温故「有用」关闭门禁 6** — 验收：`usage_report --days 1` 的表里出现 `recall_useful` 计数。
- [ ] **W0-5 判定 W1 能否于 09-23 开工** — 验收：本板 §② 六条全绿则开工；否则在 §⑤ 记一行「W1 顺延，原因 X」。

> 五条全是**关闭既有门禁**，无一条新增功能——符合 §① 承诺。

---

## ④ 风险与假设登记册（周五过一遍）

| # | 风险 / 假设 | 触发信号 | 应对 | 状态 |
|---|---|---|---|---|
| 1 | 备份仓是公开仓，vault 内含复盘/蒸馏/灵感 | 任何 `git push` 出现在备份路径 | 缺省不 push；要推改走独立私有镜像 | 🟡 已缓解，未复核 |
| 2 | `usage.local.jsonl` 被误提交 → 随 Pages 公开 | `git status` 出现该文件 | `.gitignore` 显式列名 + 测试断言 | 🟢 已钉死 |
| 3 | 量测自身变成新的空表 | 30 天 `report_run` < 4 | 删整条量测线（ADR 0015 放弃线） | 🟢 已 4 次 |
| 4 | systemd 用户服务登出被杀 | 重启后 `is-active` 非 active | `enable-linger` 已开，备选 `@reboot` | 🟢 实测 Linger=yes |
| 5 | 每小时 cron 触发 enrich 付费调用放大成本 | 账单异常 | 已核按 url 幂等缓存；换模型/清缓存时重估 | 🟡 待 cron 真跑后观察 |
| 6 | 温故池只有 1 张真卡 → 点过一次就消失两天 | W1 结束仍 < 3 张 | 先装货（每周 2 张），不够再把池子扩到「产品拆解」 | 🔴 **未缓解** |
| 7 | `--line` 令牌改色的连锁反应未穷举 1145 行 CSS | 深色截图显脏 | W3 门禁补课时重点看 | ⚪ 未到期 |
| 8 | 「每天 3 分钟契约」仍可能落空 | 任一 7 天窗口 < 4 天有 `app_use` | 无补偿手段；但**这次落空会被量到** | 🔴 **本期最大不确定性** |
| 9 | 本 PM 机制自身变成元工作 | 28 天内本板被更新 < 8 天 | 删板（指南 §8 放弃线） | 🆕 本期新增 |

---

## ⑤ 偏离记录

| 时间 | 做了什么 / 发生了什么 | 为什么 |
|---|---|---|
| 2026-09-21 | **发现 W0 未全绿**：门禁 3 完全未达成、5/6 半达成，实际完成度 2/6。而 CHANGELOG 已按「已完成」口径写 | 建板时逐条核验实况才发现。这正是本板要解决的问题——**计划宣称完成、无人逐条对过** |
| 2026-09-21 | 计划外插入「补执行层」一批（本板 + 操作指南 + `usage_report --mix`） | 用户提出「项目管理是不是没做好」并拍板重建。**属元工作，0 个产品功能**，与 §① 承诺相符（承诺本就不含新功能），但占用了 W0 收尾时间 |
| 2026-09-21 | 新增「项目进度」视图（本期第 1 个新增产品功能） | 用户要求在工作台里看进度与计划表；已改 §① 基线并记 §⑥ |
| 2026-09-21 | 计划外插入「文档规则收敛 + `check_docs.py`」：规则 3 份副本削成 1 真源（新增 `docs/文档规范.md`）+ 四项 CI 硬门 | 用户说「文档有点乱，想写份命名规范文档」。查证：规范早写全了，乱的是 3 份副本打架且零校验——正是 §3.1 台账「活文档漂移 🔴 已达升级线」那条。照 §3.0「禁止再写第 3 份文档」改为收敛+门禁。**属元工作，0 个产品功能**；台账该条已结算 ✅ |
| 2026-09-21 | 计划外插入「Stop hook 收尾门禁」：`.claude/hooks/gate.sh` + `.claude/settings.json`，agent 每轮收尾自动跑 4 道门，红了不许结束 | 用户追问「踩过的坑是不是该写个文档让 AI 先扫描」。查证：坑点台账早已存在，且「写文档让 AI 读」有实测反证（emoji 写进 5 个文档仍复发 3 次）。真缺口是**反馈时机**（门禁只在 CI、agent 看不到）。**属元工作，0 个产品功能**；实测单轮 1.25 秒 |
| 2026-09-21 | 计划外插入「AI 反向提问 10 条」对照分析存档 → `docs/research/AI反向提问10条-参考地图.md` | 用户给图问「能否优化提示词技能」。结论：该图实为**写给 AI 的回答契约**、不属 `prompt-refine`，真空位在全局 `CLAUDE.md`（零条回答质量准则）。用户拍板**只存档不改配置**。属元工作，0 个产品功能 |
| 2026-09-22 | 计划外插入「门禁清单收敛 + pre-commit 前移」：新增 `check_all.py` 为唯一清单，Stop hook / pre-commit / CI 三处全调它；台账 §3.1 拆「在办 / 已毕业」封顶人读区 | 用户问「每次提交前查质量 + 坑点写成规则文档 + 下次先扫再审」。查证该机制已存在且更成熟，真缺口是**触发点无强制力**与**台账只进不出**。当场验出新坑：昨日建的 `gate.py` 自带清单与 `check_all.py` **当天就漂**（少 flake8/tsc），已记 §3.1b 并结算。**属元工作，0 个产品功能**；用户拍板不加第三套 hook（防门禁通胀） |

---

## ⑥ 变更记录

- 2026-09-21 · **改 §① 基线（第二次）**：「明确不交」删去「dogfood 完成度仪表 / 使用统计视图」，放行「项目进度」视图（ADR 0016）。代价已在 ADR 0016 逐条写明：撞 ADR 0015 三理由、逆 R10、过不了频率门。用户看过代价后拍板。
- 2026-09-21 · **改 §① 基线**：补第 6 条「10-17 发 v0.11.0 并打 tag」。依据：用户决定发版等冲刺末，而非现在发。按指南 §4「①期中不改，要改就在 §⑥ 记一行」——这是本板第一次行使该条。
- 2026-09-21 · 建板。②③ 条目搬自 [`对表冲刺-2026-09.md`](对表冲刺-2026-09.md) 的 `W0-E` 与 W1–W4；④ 搬自其「风险与不确定点」8 条并新增第 9 条。
