# 作战板 · 对表冲刺 2026-09-20 → 10-17

> 性质：**当期数据载体**。规则见 [`../../docs/guides/项目管理-操作指南.md`](../../docs/guides/项目管理-操作指南.md)，为什么这么排见 [`对表冲刺-2026-09.md`](对表冲刺-2026-09.md)。
> 建板：2026-09-21　对表日：**2026-10-17**
> **当期完成度 = §② 勾选数 / 总数 = 2 / 6 = 33%**（最后核对 2026-09-21）
> 纪律：②③ 条目**只搬不重写**；勾 `[x]` 前逐条对 DoD 四款（指南 §3）。

---

## ① 本期承诺（基线 · 期中不改）

**交什么**：不是功能，是**「这工具真的被用了」这件事本身**。

1. 清掉「打得开、有东西、说真话」的障碍（W0）
2. 装好尺子并让它出非空数（W0）
3. 每天 3 分钟契约跑满四周，每周蒸馏 2 张真卡给温故池装货（W1–W4）
4. 按摩擦决定加什么，**每周最多一件且必须是第 3 档最小版**（W2–W3）
5. 10-17 打第二张自评卡，**触发放弃线的当场执行**（W4）

**明确不交**（搬自冲刺计划负向清单，不重写理由）：新增任何产品功能（含 R05）· Phase 3 云收件箱 · 往 `data.json` 加键 · 使用统计视图 · 新增资讯源 / 扩语义检索 / 修 X 源 · 救 self-hosted runner · 架构评审候选 2–6 · dogfood 完成度仪表 · 把报表自动写进 docs。

---

## ② 出口门禁（W0 → W1 · 完成度的分母）

> 冲刺计划原文：**6 条全绿才算 W1 可以开始**；第 6 条不过，后面三周全是白跑。

- [x] **1 · 备份仓公私已确认，备份策略已据此选定** — 验收：vault 的 origin 为公开仓，故 `vault_backup.py` 缺省**只提交不 push**，决策记于 CHANGELOG `[Unreleased]`。
- [~] **2 · 后端 systemd 自启已验证 + 浏览器启动页已固定** — 验收：`systemctl --user is-enabled/is-active workbench.service` → `enabled`/`active`，`loginctl` → `Linger=yes`（2026-09-21 实测）。**浏览器启动页在仓外，未验证**。
- [ ] **3 · crontab 两行已挂，`local_refresh.log` 出现非手动的整点记录** — 2026-09-21 实测**未达成**：`crontab -l` 无本项目条目、`systemctl --user list-timers` 为 0、日志停在 `2026-09-20 15:03:44`、`vault_backup` 从未产出日志。⚠ **CHANGELOG 已写「本机 crontab 两行」与实况不符**（见 §⑤ 偏离 1）。
- [x] **4 · ADR 0015 + 量测四件 + 测试全绿 + `bump_version.py` 已跑** — 验收：`docs/adr/0015-*.md` 在仓，四件齐（`clients/usage.py`·`pipeline/usage_report.py`·`js/core/usage.js`·`usage.local.jsonl` 已 gitignore），`pytest -q` **81 passed**（2026-09-21）。
- [~] **5 · D1 幽灵卡 / 温故空态 / D4 文案已修并真机验过** — 代码侧已改（CHANGELOG `[Unreleased]` Fixed 段 + `test_kb_list_deposits_skips_ghost_rows`）；**「真机验过」无证据**，待一次 chrome-devtools 走查。
- [~] **6 · 报表能打出非空表；点一次温故「有用」后报表第二次能看见** — 前半达成（2026-09-21 报表输出非空）；**后半未达成**：埋点至今只有 `app_use 1 / recall_open 1 / report_run 4`，**无 `recall_useful`**。

---

## ③ 本周任务（≤5 条 · 2026-09-21 → 09-22 · 补 W0 缺口）

- [ ] **W0-1 挂上两条定时任务** — 验收：`crontab -l` 见到 `local_refresh` 与 `vault_backup` 两行，且次日 `local_refresh.log` 出现**非手动的整点**记录。
- [ ] **W0-2 订正 CHANGELOG 的不实陈述** — 验收：`[Unreleased]` 里「本机 crontab 两行」改为与实况一致的措辞。
- [ ] **W0-3 真机走查关闭门禁 5** — 验收：温故卡不再出现幽灵卡、无到期卡时显示提示而非整块消失、`list_console_messages` 零新报错。
- [ ] **W0-4 点一次温故「有用」关闭门禁 6** — 验收：`usage_report --days 1` 的表里出现 `recall_useful` 计数。
- [ ] **W0-5 判定 W1 能否于 09-23 开工** — 验收：本板 §② 六条全绿则开工；否则在 §⑤ 记一行「W1 顺延，原因 X」。

> 五条全是**关闭既有门禁**，无一条新增功能——符合 §① 承诺。

---

## ④ 风险与假设登记册（周五过一遍）

| # | 风险 / 假设 | 触发信号 | 应对 | 状态 |
|---|---|---|---|---|
| 1 | 备份仓是公开仓，vault 内含复盘/蒸馏/灵感 | 任何 `git push` 出现在备份路径 | 缺省不 push；要推改走独立私有镜像 | 🟡 已缓解，未复核 |
| 2 | `usage.local.jsonl` 被误提交 → 随 Pages 公开 | `git status` 出现该文件 | `.gitignore` 显式列名 + 测试断言 | 🟢 已钉死 |
| 3 | 量测自身变成新的空表 | 30 天 `report_run` < 4 | 删整条量测线（ADR 0015 放弃线） | 🟢 已 4 次 |
| 4 | systemd 用户服务登出被杀 | 重启后 `is-active` 非 active | `enable-linger` 已开，备选 `@reboot` | 🟢 实测 Linger=yes |
| 5 | 每小时 cron 触发 enrich 付费调用放大成本 | 账单异常 | 已核按 url 幂等缓存；换模型/清缓存时重估 | 🟡 待 cron 真跑后观察 |
| 6 | 温故池只有 1 张真卡 → 点过一次就消失两天 | W1 结束仍 < 3 张 | 先装货（每周 2 张），不够再把池子扩到「产品拆解」 | 🔴 **未缓解** |
| 7 | `--line` 令牌改色的连锁反应未穷举 1145 行 CSS | 深色截图显脏 | W3 门禁补课时重点看 | ⚪ 未到期 |
| 8 | 「每天 3 分钟契约」仍可能落空 | 任一 7 天窗口 < 4 天有 `app_use` | 无补偿手段；但**这次落空会被量到** | 🔴 **本期最大不确定性** |
| 9 | 本 PM 机制自身变成元工作 | 28 天内本板被更新 < 8 天 | 删板（指南 §8 放弃线） | 🆕 本期新增 |

---

## ⑤ 偏离记录

| 时间 | 做了什么 / 发生了什么 | 为什么 |
|---|---|---|
| 2026-09-21 | **发现 W0 未全绿**：门禁 3 完全未达成、5/6 半达成，实际完成度 2/6。而 CHANGELOG 已按「已完成」口径写 | 建板时逐条核验实况才发现。这正是本板要解决的问题——**计划宣称完成、无人逐条对过** |
| 2026-09-21 | 计划外插入「补执行层」一批（本板 + 操作指南 + `usage_report --mix`） | 用户提出「项目管理是不是没做好」并拍板重建。**属元工作，0 个产品功能**，与 §① 承诺相符（承诺本就不含新功能），但占用了 W0 收尾时间 |

---

## ⑥ 变更记录

- 2026-09-21 · 建板。②③ 条目搬自 [`对表冲刺-2026-09.md`](对表冲刺-2026-09.md) 的 `W0-E` 与 W1–W4；④ 搬自其「风险与不确定点」8 条并新增第 9 条。
