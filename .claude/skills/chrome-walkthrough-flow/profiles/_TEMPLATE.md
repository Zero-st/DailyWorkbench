# profile：<项目名>（<系统全称 / 一句话定位>）

> 复制本文件为 `profiles/<项目>.md` 并**填满每个插槽**，供 `chrome-walkthrough-flow` 在该项目下走查/联调时使用。
> 守则：**找不到当前项目的 profile 就停下让用户补一个，绝不拿别项目的参数硬套。**
> 迁移自检：填完后全文 `grep -n "______\|<.*>\|【填\|TODO"` 应零残留。

## mode

**默认 `______`（`write-e2e` 端到端写路径 / 还是 `read-only-walkthrough` 只读走查？）** —— 写模式下允许哪些写操作（新增/保存/提交/审核/退回/删除/导出=______），是否逐步停下确认（______）。每步写完用什么手段核对是否真的落库/流转（______，**不看前端 toast**）。

> **调用方可覆盖为只读**：若调用方（如批量编排的 `--walkthrough-readonly`）要求只读，则跳过所有写路径，只浏览+点击导航+数据库 SELECT 核对。
>
> **缺陷复现模式（供 bugfix 类只读复现）**：只读手段复现（抓控制台/网络/快照 + SELECT 核对触发数据）；**绝不为复现去改/删存量或他人在途数据**；必须有数据才触发时只能自建测试单据；复现不了 → 停下报告"无法安全复现 + 需要什么"。

**作用域红线（务必遵守）**：写/审/退/删只作用于**本次走查自己新建的测试单据**。目标环境是否多人共享（______）——若是，**绝不**对存量数据 / 他人在途单据做不可逆操作。分不清是不是自建的、或必须动存量数据才能测 → **停下报告**。（此红线不因"自动执行"而放宽。）

### <项目>写路径流程（对自建单据自动执行）

1. **新增/保存/提交**：进哪个业务列表→新增→按需求填表→保存→提交。记住本条单据主键/编号，后续只对它操作。______
2. **审核（切角色）**：切角色方式见下「切角色」节。禁自审规则（______）；权限/隔离规则（哪些节点按组织隔离、哪些不隔离＝______）；审核自动化手法（弹窗/confirm 如何处理＝______）。
3. **退回 / 删除 / 导出**：仅对自建单据；操作后核对状态位/数据变化。
4. 每步用只读查询核对主副表与状态码。异常即记 bug。

> 业务写全走 UI；不做 DB 直接 DML/DDL（______ 是否有写拦截 hook）；结构变更不在走查里做。

## 环境 / 登录

- **走查前置**：目标环境的代码改动如何生效（热更 / 需先编译重启＝______）；若刚改了需编译的代码，走查前必须先让调用方跑对应"重建"能力（______），否则打到旧代码。
- **多实例/分流**（如有）：如何路由到"我这份改动"的后端实例（cookie/header/域名＝______），否则打到没改动的后端。
- `baseUrl`：`______`（环境类型：dev/test/... ；页面是否热加载）。
- 登录页：`______`；认证方式（纯账号密码 / CAS / SSO / OAuth＝______）。
- 登录字段（HTML name/id）：`______`；表单提交到 `______`；成功跳 `______`、失败回 `______`。
- 若被重定向到非目标环境（如生产 SSO）→ **停下报告**，不尝试异常登录。
- 验证码：如何判断出现（______）；**出现即停下请人工，不自动 OCR、不乱填**，报告记一条。
- 登录后核对请求 2xx、抓 JS 报错；失败不反复盲试（避免触发验证码/锁定），如实报告。

## 凭据来源（第一步，必做）

1. `Read` 项目内 `______`（如 `.claude/walkthrough-accounts.json`）。
2. 文件不存在/为空/仍是占位符 → **停下**，提示照 `.example` 模板填真实账号，**不要捏造账号**。
3. 结构：`______`（baseUrl + accounts[]，每个含 label/username/password/roles[]，roleId 可能运行时实测回填）。
4. 含明文密码：**绝不写进报告/输出**；提账号时脱敏。

## 切角色

- 多角色首登是否落角色选择页（______）。
- 如何枚举可切角色（读哪个下拉/接口＝______）。
- 切换方式（导航到 `______` / 触发下拉 onchange）——**优先用导航更稳**。
- **`roleId`/角色标识因环境而异，绝不硬编码**——只从配置或运行时实测获取，把映射记进报告。
- 角色面（列出主要角色＝______）。

## 数据库核对

- DB 类型 + 访问通道（______，如 Oracle MCP `query_database` 只 SELECT）。写模式下**每个写/状态切换点都要 SELECT 核对**（落库、状态码、主副表流转），不看前端 toast。绝不 DML/DDL。

## 落盘

- 报告：给了任务目录时 `Write` 落到 `______/walkthrough-report.md`（**唯一允许 Write 的文件**）并在返回文本同时给正文；没给目录只返回文本。
- 截图：存到 `______/screenshots/`；文件名带角色+页面。

## 预算

- 单次走查浏览器操作 **≤ ______ 步**；接近即收敛、立即出报告。
